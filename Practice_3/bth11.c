#include<stdio.h>
main()
{
	int
	printf("Nhap n: ");
	scanf("%d", &n);
	if(n >= 1)
	{
		if(n >= 1 && n <100)
		{
			while()
			{
				i = n%10;
				
			}
		}
		else
		{
			for(; ; )
		}
	}
	else
		printf("Nhap lai!");
}
